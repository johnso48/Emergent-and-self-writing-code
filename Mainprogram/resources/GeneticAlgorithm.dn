uses pal.control.RestAPI
uses tree.treeConstructor
uses Knowledge.KnowledgeBase

data Children{
	String config[]
	dec fitness
}

interface GeneticAlgorithm{

	char[] call(RestAPI restAPI, interfaceLevels interfaces[], KnowledgeBase kb)
		
}