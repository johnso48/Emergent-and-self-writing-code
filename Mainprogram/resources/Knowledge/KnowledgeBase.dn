uses pal.control.RestAPI


data knowledge{
	dec rewardValue
	char config[]
	char workLoad[]
	dec averageSize
}

interface KnowledgeBase{

	KnowledgeBase(RestAPI restapi)
	bool inKnowledgeBase(char config[])
	dec testNewConfig(char config[])

}
